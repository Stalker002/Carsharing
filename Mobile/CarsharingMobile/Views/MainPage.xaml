<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CarsharingMobile.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:cars="clr-namespace:Shared.Contracts.Cars;assembly=Shared"
    xmlns:controls="clr-namespace:CarsharingMobile.Extensions"
    xmlns:maps="clr-namespace:Microsoft.Maui.Controls.Maps;assembly=Microsoft.Maui.Controls.Maps"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:vm="clr-namespace:CarsharingMobile.ViewModels"
    x:DataType="vm:MainViewModel"
    Shell.NavBarIsVisible="False">

    <Grid>
        <maps:Map
            x:Name="CarMap"
            IsShowingUser="True"
            ItemsSource="{Binding Cars}"
            MapClicked="OnMapClicked">
            <maps:Map.ItemTemplate>
                <DataTemplate x:DataType="cars:CarWithMinInfoDto">

                    <controls:CarPin
                        Address="{Binding Model}"
                        ImageUrl="{Binding ImagePath}"
                        Label="{Binding Brand}"
                        Latitude="{Binding Latitude}"
                        Longitude="{Binding Longitude}"
                        MarkerClicked="OnCarPinClicked" />

                </DataTemplate>
            </maps:Map.ItemTemplate>
        </maps:Map>

        <Grid Padding="20,40,20,40" InputTransparent="False">

            <Border
                BackgroundColor="White"
                HeightRequest="50"
                HorizontalOptions="Start"
                StrokeShape="RoundRectangle 25"
                StrokeThickness="0"
                VerticalOptions="Start"
                WidthRequest="50">
                <Border.Shadow>
                    <Shadow
                        Brush="Black"
                        Opacity="0.2"
                        Radius="5"
                        Offset="0,2" />
                </Border.Shadow>
                <ImageButton
                    Padding="12"
                    Command="{Binding OpenMenuCommand}"
                    Source="menu_icon.png" />
            </Border>

            <VerticalStackLayout
                HorizontalOptions="End"
                Spacing="15"
                VerticalOptions="Center">
                <Border
                    BackgroundColor="White"
                    StrokeShape="RoundRectangle 25"
                    StrokeThickness="0"
                    WidthRequest="50">
                    <Border.Shadow>
                        <Shadow
                            Brush="Black"
                            Opacity="0.2"
                            Radius="5"
                            Offset="0,2" />
                    </Border.Shadow>
                    <VerticalStackLayout>
                        <Button
                            Padding="0,0,0,0"
                            BackgroundColor="Transparent"
                            Clicked="OnZoomInClicked"
                            FontSize="24"
                            HeightRequest="50"
                            Text="+"
                            TextColor="#112347" />
                        <BoxView
                            Margin="10,0"
                            HeightRequest="1"
                            Color="#E1E1E1" />
                        <Button
                            Padding="0,0,0,10"
                            BackgroundColor="Transparent"
                            Clicked="OnZoomOutClicked"
                            FontSize="24"
                            HeightRequest="50"
                            Text="-"
                            TextColor="#112347" />
                    </VerticalStackLayout>
                </Border>

                <Border
                    BackgroundColor="White"
                    HeightRequest="50"
                    StrokeShape="RoundRectangle 25"
                    StrokeThickness="0"
                    WidthRequest="50">
                    <Border.Shadow>
                        <Shadow
                            Brush="Black"
                            Opacity="0.2"
                            Radius="5"
                            Offset="0,2" />
                    </Border.Shadow>
                    <ImageButton
                        Padding="12"
                        Clicked="OnMyLocationClicked"
                        Source="my_location_icon.png" />
                </Border>

            </VerticalStackLayout>
        </Grid>

        <Border
            Padding="20,10,20,30"
            BackgroundColor="White"
            IsVisible="{Binding IsCardVisible}"
            StrokeShape="RoundRectangle 20,20,0,0"
            StrokeThickness="0"
            VerticalOptions="End">
            <Border.Shadow>
                <Shadow
                    Brush="Black"
                    Opacity="0.3"
                    Radius="10"
                    Offset="0,-2" />
            </Border.Shadow>
            <Border.GestureRecognizers>
                <SwipeGestureRecognizer Command="{Binding CloseCardCommand}" Direction="Down" />
            </Border.GestureRecognizers>
            <VerticalStackLayout Spacing="15">
                <BoxView
                    Margin="0,0,0,10"
                    CornerRadius="2"
                    HeightRequest="4"
                    HorizontalOptions="Center"
                    WidthRequest="40"
                    Color="#E1E1E1">
                    <BoxView.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CloseCardCommand}" />
                    </BoxView.GestureRecognizers>
                </BoxView>
                <Grid ColumnDefinitions="120, *" ColumnSpacing="15">
                    <Image
                        Grid.Column="0"
                        Aspect="AspectFit"
                        HeightRequest="80"
                        Source="{Binding SelectedCar.ImagePath, TargetNullValue='car_placeholder.png'}" />
                    <VerticalStackLayout
                        Grid.Column="1"
                        Spacing="2"
                        VerticalOptions="Center">
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            TextColor="Black">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="{Binding SelectedCar.Brand}" />
                                    <Span Text=" " />
                                    <Span Text="{Binding SelectedCar.Model}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                        <Label
                            FontSize="13"
                            Text="{Binding SelectedCar.Transmission}"
                            TextColor="#999" />
                    </VerticalStackLayout>
                </Grid>
                <Grid Padding="10,0" ColumnDefinitions="*, *">
                    <VerticalStackLayout Grid.Column="0" Spacing="0">
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            HorizontalOptions="Center"
                            Text="{Binding SelectedCar.PricePerDay, StringFormat='{0} byn/день'}"
                            TextColor="Black" />
                    </VerticalStackLayout>
                    <VerticalStackLayout
                        Grid.Column="1"
                        Spacing="2"
                        VerticalOptions="Center">
                        <Label
                            FontAttributes="Bold"
                            FontSize="16"
                            HorizontalOptions="Center"
                            Text="{Binding SelectedCar.MaxFuel, StringFormat='{0} л.'}"
                            TextColor="Black" />
                        <Label
                            FontSize="11"
                            HorizontalOptions="Center"
                            Text="ОБЪЕМ БАКА"
                            TextColor="#999" />
                    </VerticalStackLayout>
                </Grid>
                <Button
                    Margin="0,10,0,0"
                    BackgroundColor="#76C54A"
                    Command="{Binding BookCarCommand}"
                    CornerRadius="8"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="55"
                    Text="ЗАБРОНИРОВАТЬ"
                    TextColor="White" />
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
